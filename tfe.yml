extends: default
heading:
  min-height-after: auto
footer:
  height: 0.45in
  recto:
    right:
      content: '{section-or-chapter-title} | {page-number}'
  verso:
    right:
      content: $footer_recto_right_content
    left:
      content: ''